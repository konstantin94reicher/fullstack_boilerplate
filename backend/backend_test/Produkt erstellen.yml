info:
  name: Produkt erstellen
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/products
  body:
    type: json
    data: |-
      {
        "name": "Monitor",
        "price": 349
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
