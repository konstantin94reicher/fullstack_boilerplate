info:
  name: FEHLER - Produkt erstellen
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/products
  body:
    type: json
    data: |-
      {
        "name": "Monitor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
